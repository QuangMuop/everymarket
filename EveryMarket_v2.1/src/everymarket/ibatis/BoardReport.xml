<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="BoardReport">
	
	<typeAlias alias="BoardReport" type="everymarket.model.BoardReport"/>

	<select id="getReportedProduct" resultClass="BoardReport">
		select *
		from board_report
		where r_check = 'n' and not r_productid = -1
		order by r_regdate desc
	</select>
	
	<select id="getReportedMember" resultClass="BoardReport">
		select *
		from board_report
		where r_check = 'n' and r_productid = -1 
		order by r_regdate desc
	</select>

	<select id="getUncheckedReportList" resultClass="BoardReport">
		select *
		from board_report
		where r_check = 'n'
	</select>

	<update id="checkReportStatus">
		update board_report
		set r_check = 'y'
		where r_id = #r_id#
	</update>

</sqlMap>